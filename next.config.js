
/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  trailingSlash: true,
  distDir: 'build',
  reactStrictMode: true,
};

if (process.env.NODE_ENV !== 'development') {
  nextConfig.assetPrefix = '/Frontend_Mentor/advice-generator-app-main';
  nextConfig.basePath = '/Frontend_Mentor/advice-generator-app-main';
}


module.exports = nextConfig;
